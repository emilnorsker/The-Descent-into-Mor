extends Node

# Entity Colors
const PLAYER = Color.CORNFLOWER_BLUE
const ENEMY = Color.INDIAN_RED
const ITEM = Color.YELLOW
const WALL = Color.GRAY
const FLOOR = Color.DARK_GRAY
const STAIRS = Color.GREEN
const WHITE = Color.WHITE

# Message Colors
const PLAYER_ATTACK = Color.LIGHT_GRAY
const ENEMY_ATTACK = Color.LIGHT_SALMON
const STATUS_EFFECT_APPLIED = Color.LIME_GREEN
const DESCEND = Color.MEDIUM_PURPLE
const PLAYER_DIE = Color(0.9, 0.1, 0.1, 1.0)
const ENEMY_DIE = Color(0.6, 0.3, 0.1, 1.0)

# UI Colors
const INVALID = Color(0.8, 0.8, 0.0, 1.0)
const IMPOSSIBLE = Color(0.6, 0.6, 0.6, 1.0)
const ERROR = Color(0.8, 0.0, 0.0, 1.0)
const WELCOME_TEXT = Color(0.2, 0.8, 0.2, 1.0)
const HEALTH_RECOVERED = Color(0.0, 0.8, 0.0, 1.0)

# Item Colors
const HEALTH_POTION = Color.PURPLE
const SWORD = Color.GRAY
const DAGGER = Color.GRAY
const LEATHER_ARMOR = Color.ORANGE
const CHAINMAIL = Color.GRAY
const FIREBALL_SCROLL = Color.RED
const LIGHTNING_SCROLL = Color.YELLOW
const CONFUSION_SCROLL = Color.MAGENTA

# UI Theme Colors
const UI_TEXT = Color.WHITE
const UI_TEXT_DISABLED = Color.GRAY
const UI_OUTLINE = Color.BLACK
const UI_BORDER = Color.WHITE
const UI_TRANSPARENT = Color(1, 1, 1, 0)
const UI_HEALTH_BAR_BG = Color(0.5, 0, 0, 1)
const UI_HEALTH_BAR_FG = Color(0, 0.5, 0, 1)
const UI_XP_BAR_BG = Color(0, 0, 0.5, 1)
const UI_XP_BAR_FG = Color(0, 0, 1, 1)
const UI_TITLE_TEXT = Color.YELLOW

# Game colors
const PLAYER_ATK = Color(0.9, 0.2, 0.2, 1.0)  # Light red
const ENEMY_ATK = Color(0.9, 0.6, 0.1, 1.0)   # Orange
const BAR_TEXT = Color(1.0, 1.0, 1.0, 1.0)    # White
const BAR_FILLED = Color(0.0, 0.8, 0.0, 1.0)  # Green
const BAR_EMPTY = Color(0.8, 0.0, 0.0, 1.0)   # Red
const HEALTH_XP = Color(0.0, 0.8, 0.0, 1.0)   # Green 